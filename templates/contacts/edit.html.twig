{% extends 'base.html.twig' %}

{% block title %}Edition Contact{% endblock %}

{% block content %}
    <h1 class="erp-family">Edition Contact</h1>
    {% for message in app.flashes('alert') %}
        <div class="alert alert-info">
            {{ message }}
        </div>
    {% endfor %}

    {{ include('contacts/form_edit.html.twig', {'button_label': 'Sauvegarde'}) }}

    <a href="{{ path('contacts_index') }}" class="btn btn-primary btn-sm mt-1 mb-1 erp-family">Retour Liste</a>

    {{ include('contacts/_delete_form.html.twig') }}

{% endblock %}
