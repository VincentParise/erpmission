{% extends 'base.html.twig' %}

{% block title %}Edition Mission{% endblock %}

{% block content %}
    <h1 class="erp-family">Edition Mission</h1>

    {% for message in app.flashes('alert') %}
        <div class="alert alert-info">
            {{ message }}
        </div>
    {% endfor %}

        {{ include('missions/form_edit.html.twig', {'button_label': 'Sauvegarder'}) }}


        {{ include('missions/_delete_form.html.twig') }}


    <a href="{{ path('missions_index') }}" class="btn btn-primary btn-sm mt-1 erp-family" >Retour liste</a>





{% endblock %}
