{% extends 'base.html.twig' %}

{% block title %}Création Missions{% endblock %}

{% block content %}
    <h1>Création nouvelle Mission</h1>

    {% for message in app.flashes('alert') %}
        <div class="alert alert-info">
            {{ message }}
        </div>
    {% endfor %}

        {{ include('missions/form.html.twig', {'button_label': 'Sauvegarder'}) }}


        {{ include('missions/_delete_form.html.twig') }}


    <a href="{{ path('missions_index') }}" class="btn btn-primary btn-sm mt-1">Retour liste</a>





{% endblock %}
