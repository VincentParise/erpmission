<div class="erp-family">
    {{ form_start(form) }}

    <div class="row mb-3 mt-5">
      <div class="col form-group d-flex d-inline">
          <div class="px-2 align-self-center">{{ form_label(form.agents,'Agents') }}</div>
          <div>{{ form_widget(form.agents) }}</div>
      </div>

    </div>
    <hr>

    <button class="btn btn-info btn-sm mt-1 mb-1">{{ button_label|default('Sauvegarde') }}</button>

{{ form_end(form) }}
