<form method="post" action="{{ path('missions_delete', {'id': mission.id}) }}" onsubmit="return confirm('Etes vous sûr de vouloir supprimer cette mission ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mission.id) }}">
    <button class="btn btn-danger btn-sm erp-family">Supprimer</button>
</form>
