{% extends 'base.html.twig' %}

{% block title %}Edit Planques des Missions{% endblock %}

{% block content %}
    <h1 class="erp-family">Planque mission : {{ mission.codename }}</h1>

    {% for message in app.flashes('alert') %}
        <div class="alert alert-info">
            {{ message }}
        </div>
    {% endfor %}

    {{ include('missions/form_edit_planques.html.twig', {'button_label': 'Sauvegarde'}) }}

        {# {{ include('missions/delete_planques_form.html.twig') }} #}

    <a href="{{ path('missions_index') }}" class="btn btn-primary btn-sm mb-5">Retour Liste</a>

{% endblock %}
